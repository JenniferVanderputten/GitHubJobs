<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mab_menu"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/mab_menu_add"
        android:icon="@mipmap/sym_action_add"
        android:title="@string/add_search_hint"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/mab_menu_search"
        android:icon="@mipmap/ic_menu_search_holo_dark"
        android:title="@string/search_page_hint"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/mab_menu_location"
        android:icon="@mipmap/perm_group_location"
        android:title="@string/get_location_hint"
        app:showAsAction="ifRoom"/>

</menu>